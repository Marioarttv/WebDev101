# 📚 Beispiel-Dateien für Schüler

Hier findest du Beispiele, die du als Vorlage oder Inspiration für deine eigene Website nutzen kannst.

## 📁 Dateien:

### 1. `01-grundgeruest.html`
Das absolute Minimum - so sieht jede HTML-Seite aus.
- Zeigt die Basis-Struktur
- Nur eine Überschrift und ein Absatz
- Perfekt zum Start!

### 2. `02-ueber-mich.html`
Eine einfache "Über mich" Seite ohne CSS.
- Nutzt verschiedene HTML-Tags:
  - Überschriften (`<h1>`, `<h2>`)
  - Absätze (`<p>`)
  - Listen (`<ul>`, `<ol>`)
  - Links (`<a>`)

### 3. `03-mit-css.html` + `style.css`
Die gleiche Seite, aber mit schönem Design!
- Externe CSS-Datei eingebunden
- Farben, Abstände, Rahmen
- Button-Styling
- Responsive Design

## 🎯 So nutzt du die Beispiele:

1. **Anschauen:** Öffne die HTML-Dateien im Browser
2. **Code lesen:** Öffne die Dateien in VS Code
3. **Ausprobieren:** Kopiere Code-Teile für dein Projekt
4. **Verändern:** Experimentiere mit den Werten in der CSS-Datei

## 💡 Übungen:

### Einfach:
- ✏️ Ändere die Texte in `02-ueber-mich.html` zu deinen eigenen Infos
- 🎨 Ändere in `style.css` die Farben (z.B. `#667eea` zu `#ff6b6b`)

### Mittel:
- ➕ Füge ein Bild zu deiner Seite hinzu (`<img>` Tag)
- 📝 Erstelle eine zweite Seite und verlinke sie

### Fortgeschritten:
- 🎯 Baue eine Navigation mit mehreren Seiten
- 🎨 Erstelle dein eigenes Farbschema
- 📱 Teste und verbessere das Responsive Design

## 🤔 Fragen?

- Nutze die W3Schools Website für Erklärungen
- Frage Claude oder ChatGPT
- Experimentiere und hab Spaß! 🚀
